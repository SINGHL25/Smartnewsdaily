### premium_page.dart
```dart
class PremiumPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Premium News")),
      body: Center(child: Text("Subscribe to unlock premium articles.")),
    );
  }
}
```
